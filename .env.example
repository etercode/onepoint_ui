# Browser (and Vite dev) — used for client-side fetches
VITE_API_URL=http://localhost:8088

# Server-side SSR only — use when the API is on a different host than the UI
# (Docker: http://onepoint-api:8088, production: internal service URL)
API_URL=http://localhost:8088
